# Tasks: Script System Standardization And Governance

**Stage:** INFRA-025
**Phase:** 01_PLATFORM_FOUNDATION
**Tasks Total:** 14
**Generated:** 2026-03-21T12:05:05Z

---

## Task List

- [ ] T001 [SETUP] Create migration map `docs/scripts/SCRIPT_MIGRATION_MAP.md` — full 33-entry table (Type A: 3 wrong-action-structure renames, Type B: 7 hyphen-in-domain renames, Type C: 6 no-domain-prefix renames, Type D: 8 non-allowed-domain renames, Type E: 9 redundant aliases to remove) sourced verbatim from `research.md` §3 and §Migration Design; table columns: Old Name | Type | Violation | New Name
- [ ] T002 [SETUP] Create placeholder `docs/scripts/SCRIPT_REGISTRY.md` — stub with header `# Zidney Script Registry` and notice `> Auto-generated by bun run dev:generate:script-docs. Do not edit manually.`; will be overwritten by generator in T007
- [ ] T003 [P] [SETUP] Implement `scripts/validate/script-naming.ts` with package.json entry `validate:script:naming`; enforce 9 allowed domains (`db|arch|validate|ai|ci|repo|dev|infra|test`); regex `/^(db|arch|validate|ai|ci|repo|dev|infra|test):[a-z][a-z0-9-]*(:[a-z][a-z0-9-]*)?$/`; skip lifecycle-exempt names; report-all mode (collect all violations before exit 1); add full metadata header (`@script validate:script:naming`, `@domain validate`, `@category governance`, `@description`, `@usage`); also create `scripts/validate/__tests__/script-naming.test.ts`
- [ ] T004 [P] [SETUP] Implement `scripts/validate/script-usage.ts` with package.json entry `validate:script:usage`; scan pattern `/bun run ([\w:.-]+)/g` across `.json`, `.ts`, `.yml`, `.yaml`, `.md`, `.sh` in defined glob scope; build reference set from all package.json files; report-all mode (collect all broken references before exit 1); add full metadata header (`@script validate:script:usage`, `@domain validate`, `@category governance`, `@description`, `@usage`); also create `scripts/validate/__tests__/script-usage.test.ts`
- [ ] T005 [P] [SETUP] Implement `scripts/validate/script-infrastructure.ts` with package.json entry `validate:script:infrastructure`; check all 5 metadata fields (`@script`, `@domain`, `@category`, `@description`, `@usage`) on every `.ts` under `scripts/`; regenerate `SCRIPT_REGISTRY.md` in-memory; strip `> Last generated:` line from both versions; diff normalized content; exit 1 on any metadata gap or registry staleness; add full metadata header; also create `scripts/validate/__tests__/script-infrastructure.test.ts`
- [ ] T006 [SETUP] Implement `scripts/dev/refactor-scripts.ts` with package.json entry `dev:refactor:scripts`; parse `docs/scripts/SCRIPT_MIGRATION_MAP.md` for old→new map; glob scan scope: `**/package.json`, `scripts/**/*.ts`, `.github/workflows/*.{yml,yaml}`, `docs/**/*.md`, `specs/**/*.md`, `.agents/**/*.md`, `scripts/**/*.sh`, `scripts/*.sh`; exclude `node_modules/**`, `coverage/**`, `dist/**`, `.git/**`; replace via `content.replaceAll("bun run ${oldName}", "bun run ${newName}")`; support `--dry-run` flag; re-scan for remnants after replacement, exit 1 on any remaining; write `reports/SCRIPT_REFACTOR_REPORT.md`; add full metadata header
- [ ] T007 [SETUP] Update `scripts/generate/script-docs.ts` — enforce 5-field metadata schema (`@script`, `@domain`, `@category`, `@description`, `@usage`); update own `@script` header to `dev:generate:script-docs`; add missing `@category: dev` and `@usage: bun run dev:generate:script-docs` fields; output format: domain-sorted markdown table with columns Script Name | Source File | Category | Description | Usage; write `docs/scripts/SCRIPT_REGISTRY.md` with `> Last generated:` timestamp line
- [ ] T008 [SETUP] Add 5 new governed entries to root `package.json`: `"validate:script:naming": "bun scripts/validate/script-naming.ts"`, `"validate:script:usage": "bun scripts/validate/script-usage.ts"`, `"validate:script:infrastructure": "bun scripts/validate/script-infrastructure.ts"`, `"dev:refactor:scripts": "bun scripts/dev/refactor-scripts.ts"`, `"dev:generate:script-docs": "bun run scripts/generate/script-docs.ts"` (this renames the existing `generate-script-docs` entry — remove the old key, add the new one)
- [ ] T009 [P] [SETUP] Add missing `@category` and `@usage` metadata fields to all scripts lacking them per plan.md §Scripts Requiring Metadata Header Additions — 13 files: `scripts/ai-guard.ts`, `scripts/infra-audit.ts`, `scripts/architecture-diff.ts`, `scripts/generate-ai-context.ts`, `scripts/gitnexus-context.ts`, `scripts/type-safety-guard.ts`, `scripts/check-store-cycles.ts`, `scripts/run-local-ci.ts` (Format B scripts: replace narrative header with full 5-field canonical header); `scripts/db/pool-status.ts`, `scripts/db/validate-licenses.ts`, `scripts/validate/runtime-scripts.ts`, `scripts/validate/detect-broken-scripts.ts`, `scripts/generate/script-docs.ts` (Format A scripts: add `@category` and `@usage` to existing header); sweep `scripts/ai-engine/`, `scripts/architecture-guard/`, `scripts/architecture-health/`, `scripts/dev/` for any further missing fields
- [ ] T010 [P] [SETUP] Apply all 33 renames/removals from `docs/scripts/SCRIPT_MIGRATION_MAP.md` to root `package.json` — Type A (3): `db:pool-status`→`db:status:pool`, `db:validate-licenses`→`db:validate:licenses`, `arch:validate-brain`→`arch:validate:brain`; Type B (7): `ai-context:generate`→`ai:context:generate`, `ai-context:refresh`→`ai:context:refresh`, `ai-context:validate`→`ai:context:validate`, `ai-context:status`→`ai:context:status`, `ai-runtime:status`→`ai:runtime:status`, `ai-runtime:refresh`→`ai:runtime:refresh`, `ai-runtime:validate`→`ai:runtime:validate`; Type C (6): `validate-runtime-scripts`→`validate:runtime:scripts`, `ai-guard`→`ai:guard`, `type-safety-guard`→`arch:type-safety-guard`, `seed-dashboard-test-data`→`dev:seed:dashboard-test-data`, `run-staging-smoke-tests`→`ci:smoke:staging`; Type D (8): `gitnexus:context`→`arch:gitnexus:context`, `gitnexus:validate`→`arch:validate:gitnexus`, `hygiene:report`→`dev:hygiene:report`, `maintenance:cache-clean`→`infra:cache:clean`, `check:tsconfig`→`validate:tsconfig`, `check:store-cycles`→`arch:check:store-cycles`, `infra-audit:check`→`arch:audit:check`, `generate:ai-context` → remove (duplicate of `ai:context:generate`); Type E (9): remove aliases `infra-audit`, `migrate`, `validate:architecture`, `type-coverage`, `cache-clean`, `biome`, `tsc`, `vitest`, `worker`; also update `@script` header value in `scripts/validate/runtime-scripts.ts` from `validate-runtime-scripts` to `validate:runtime:scripts`; update `scripts/validate/__tests__/runtime-scripts.test.ts` references from old name to new name
- [ ] T011 [SETUP] Run `bun run dev:refactor:scripts` live (no `--dry-run`); verify `reports/SCRIPT_REFACTOR_REPORT.md` shows `Unresolved references: 0`; manually fix any remaining stale `bun run <old-name>` references surfaced in `docs/`, `specs/`, `.agents/`, `.github/workflows/` that the engine missed due to format variations; re-run engine to confirm clean state
- [ ] T012 [SETUP] Append 4-step Script System Governance block to `.github/workflows/architecture-governance.yml` after the existing step 13 — step 14: `Validate Script Naming Convention` (`bun run validate:script:naming`), step 15: `Validate Script Usages (no broken or orphan references)` (`bun run validate:script:usage`), step 16: `Validate Script Infrastructure (headers + registry freshness)` (`bun run validate:script:infrastructure`), step 17: `Verify Script Registry Generation` (`bun run dev:generate:script-docs`); all steps: `continue-on-error: false` (default); preceded by `# ── Script System Governance ──` section comment
- [ ] T013 [SETUP] Update `.agents/skills/script-system-governance/SKILL.md` — add: (1) finalized 9-domain map table (`db`, `arch`, `validate`, `ai`, `ci`, `repo`, `dev`, `infra`, `test`) with responsibilities; (2) canonical 5-field metadata header format with `@category` enum values (`runtime`, `governance`, `dev`, `ci`, `infra`, `test`); (3) Workflow 1 — Add New Script (create file → add metadata header → add `package.json` entry → run `dev:generate:script-docs` → run `validate:script:naming`); (4) Workflow 2 — Rename Existing Script (update migration map → run `dev:refactor:scripts` → update `@script` header → run `dev:generate:script-docs` → run all three validators); (5) anti-patterns table (7 rows: inline shell, ts-node/tsx, missing header, non-allowed domain, rename without refactor engine, forget registry update, duplicate governed name)
- [ ] T014 [SETUP] Run full validation gate — script governance gate: `bun run validate:script:naming && bun run validate:script:usage && bun run validate:script:infrastructure && bun run dev:generate:script-docs` (all must exit 0 and `docs/scripts/SCRIPT_REGISTRY.md` must show no diff); then full CI gate: `bun scripts/ai-guard.ts && bun scripts/infra-audit.ts && bun run lint && bun run typecheck && bun run test`; fix any remaining violations before closing stage

---

## Execution Notes

**Sequential dependencies:**

- T001 must complete before T006 (refactor engine reads `SCRIPT_MIGRATION_MAP.md`)
- T002 must complete before T005 (infrastructure validator compares against `SCRIPT_REGISTRY.md`)
- T003, T004, T005 are independent implementations — marked `[P]`, implement concurrently
- T006 depends on T003 + T004 + T005 (validation scripts must exist before being added to the package.json entry list — though T008 is when they are registered)
- T007 depends on T005 (infrastructure validator checks generator output)
- T008 must complete before T009/T010 can be verified (new script entries must exist in `package.json` for the validators to resolve them)
- T009 and T010 are independent of each other — marked `[P]`: T009 touches script source files, T010 touches `package.json` keys; both can proceed concurrently after T008
- T011 depends on T008 + T010 (all renames must be in `package.json` before running the live refactor)
- T012 depends on T008 (CI steps reference package.json script names that must exist)
- T013 can proceed after T008 (skill documents the finalized script names)
- T014 is the terminal gate — depends on all prior tasks complete

**Parallelism groups:**

| Group | Tasks            | Condition                               |
| ----- | ---------------- | --------------------------------------- |
| A     | T001, T002       | Start immediately (no dependencies)     |
| B     | T003, T004, T005 | Start after T001 + T002 complete        |
| C     | T006, T007       | Start after T003 + T004 + T005 complete |
| D     | T008             | Start after T006 + T007 complete        |
| E     | T009, T010       | Start after T008 complete (parallel)    |
| F     | T011             | Start after T009 + T010 complete        |
| G     | T012, T013       | Start after T011 complete (parallel)    |
| H     | T014             | Start after T012 + T013 complete        |

**Blocking gate:** T014 must exit 0 on all checks before the stage can be marked CLOSED. Any failure in `validate:script:naming`, `validate:script:usage`, or `validate:script:infrastructure` blocks stage closure per FR-010.

**Shell script scope reminder:** The refactor engine (T006) and usage validator (T004) MUST include `\.sh$` in their file-match patterns. The engine skeleton in the stage file omits `.sh` — this is a known gap that must be corrected during implementation per spec clarification (2026-03-21).

**Idempotency:** The refactor engine (T006/T011) is safe to re-run; plain `replaceAll` on already-migrated content produces zero replacements. Re-running `dev:generate:script-docs` is also idempotent (only timestamp changes, normalized diff is empty).
