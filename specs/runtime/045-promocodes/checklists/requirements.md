# Requirements Checklist — Stage 45: Promocodes

Generated by: speckit.specify / Step 1
Stage: STAGE 45 – Promocodes
Date: 2026-04-05

---

## Architecture Governance

- [x] No cross-tenant access declared (database-per-tenant; no cross-workspace promocodes)
- [x] No middleware bypass declared (license + auth middleware mandatory on all workspace routes)
- [x] Tenant resolver (not direct DB instantiation) declared
- [x] No weakening of transaction boundaries declared (usage + subscription in SERIALIZABLE tx)
- [x] Server-authoritative time declared for all validity window checks (ADR-0006)
- [x] Version compatibility / forward-only migrations declared (ADR-0003; migration 024)
- [x] All writes transactional declared (promocode_usages + subscriptions in single tx)
- [x] Code immutability declared (code/type/value immutable after creation)
- [x] Usage history preservation declared (promocode_usages rows never deleted)

## Trust Chain

- [x] Tenant isolation is first gate
- [x] License validation before workspace operation
- [x] Authentication after tenant resolution
- [x] Attempt engine: not applicable (declared)
- [x] Runtime enforces server time for validity window comparisons
- [x] No client-calculated discount accepted — server recalculates on every apply
- [x] Double-validation inside transaction (revalidate under lock before usage insert)

## Import Boundaries

- [x] `apps/*` → `packages/*` only (no cross-app imports)
- [x] No UI → DB schema imports
- [x] No `packages/*` → `apps/*` imports
- [x] `domain-core/promocodes` → `domain-core/subscriptions` (type reference only)
- [x] `domain-core/promocodes` → `domain-core/plans` (type reference only)

## Functional Completeness

- [x] Promocode creation defined (FR-01) — all field constraints, type-conditional validation
- [x] Promocode listing defined (FR-02) — filters, pagination, usage count included
- [x] Promocode get defined (FR-03) — single code with analytics
- [x] Promocode deactivation defined (FR-04) — irreversible, history preserved
- [x] Code immutability defined (FR-05) — no PATCH endpoint for data fields
- [x] All 7 validation checks defined (FR-06) — fail-fast, server-only, DB NOW()
- [x] Discount calculation defined for all 3 types (FR-07) — PERCENTAGE, FIXED, FREE_TRIAL
- [x] Stacking rules defined (FR-08) — deterministic order, cumulative cap at plan_price
- [x] Usage recording transactional (FR-09) — SERIALIZABLE tx with subscription creation
- [x] Per-user limit enforcement defined (FR-10) — race-condition-safe with FOR UPDATE lock
- [x] Global usage limit enforcement defined (FR-11) — race-condition-safe with FOR UPDATE lock
- [x] Targeting enforcement defined (FR-12) — division OR group, OR-based match
- [x] Subscription flow integration defined (FR-13) — Stage 44 extend; gateway re-validation
- [x] Analytics reporting defined (FR-14) — aggregate + per-code; MMC exclusion declared
- [x] Expiration behavior defined (FR-15) — expired codes rejected, history queryable

## Database Schema

- [x] `promocodes` table defined with all columns and constraints
- [x] `promocode_usages` table defined with all columns and constraints
- [x] Case-insensitive unique index on `LOWER(code)` defined
- [x] Index for active codes within validity window defined
- [x] Index on `(student_id, promocode_id)` for per-user count defined
- [x] Unique index on `(promocode_id, subscription_id)` to prevent double-apply defined
- [x] FK constraints on `promocode_usages` (promocode_id, student_id, subscription_id) defined
- [x] Column-level type constraints (type IN, value conditionals) documented
- [x] Forward-only migration defined (024, follows 023 from Stage 44)
- [x] No shared promocode table across tenants confirmed (database-per-tenant)

## Validation Engine

- [x] 7 validation checks numbered and described in order
- [x] Fail-fast behavior declared
- [x] Error code per failing check defined
- [x] Check 3 split into NOT_YET_VALID vs EXPIRED
- [x] Check 7 targeting semantics defined (OR-based division/group match)
- [x] Stacking check defined as conditional (only when subscription already has a promo)
- [x] Validator declared as pure function (no DB access directly; counts passed in)

## Discount Calculator

- [x] PERCENTAGE formula defined (rounded down to 2 dp, clamped to 0)
- [x] FIXED formula defined (clamped to 0)
- [x] FREE_TRIAL formula defined (final_price = 0, free_trial_days returned)
- [x] FREE_TRIAL billing_type constraint declared (recurring plans only)
- [x] Calculator declared as pure function (no side effects, no DB access)
- [x] Stacked cumulative discount cap at plan_price defined

## API Contract

- [x] All promocode routes defined with method, path, handler, and description
- [x] Read-only validate endpoint defined (no usage recorded, preview only)
- [x] Deactivate uses POST (not DELETE, to align with irreversible semantic)
- [x] Analytics endpoint defined with response shape
- [x] Stage 44 `create-subscription.ts` extension defined
- [x] Gateway callback re-validation defined
- [x] Error codes defined for all 13 failure scenarios

## Validation Schemas

- [x] `createPromocodeBodySchema` with conditional type/value validation listed
- [x] `promtcodeIdParamsSchema` listed
- [x] `validatePromocodeBodySchema` listed
- [x] `listPromocodesQuerySchema` listed
- [x] `createSubscriptionWithPromoBodySchema` (Stage 44 extension) listed

## Domain Module Structure

- [x] `promocodes.types.ts` — all types defined (PromocodeRow, PromocodeInput, PromocodeValidationContext, DiscountResult, PromocodeUsageRow)
- [x] `promocodes.errors.ts` listed
- [x] `promocodes.repository.ts` — all repository functions listed
- [x] `promocodes.validator.ts` — pure validation function defined
- [x] `promocodes.calculator.ts` — pure calculator function defined
- [x] `promocodes.service.ts` — PromocodeService interface defined with all methods
- [x] `index.ts` public exports listed

## Test Requirements

- [x] Unit tests for validator (13 cases covering all 7 checks + stacking)
- [x] Unit tests for calculator (8 cases including stacked discounts)
- [x] Unit tests for service (5 cases)
- [x] Integration tests for all CRUD routes (8 cases)
- [x] Integration tests for validate endpoint (8 cases)
- [x] Integration tests for subscription integration (11 cases)
- [x] Tenant isolation test declared

## Files Manifest

- [x] Migration file listed
- [x] Drizzle schema files listed (2 new)
- [x] Domain module files listed (10 new)
- [x] Validation schema file listed (1 new)
- [x] API route files listed (8 new)
- [x] Modified files listed (3 Stage 44 extensions + domain core index + schema barrel)

## Integration Points

- [x] Stage 44 subscriptions integration documented
- [x] Stage 44 plans integration documented (FREE_TRIAL billing_type check)
- [x] Stage 42 students integration documented (division_id, group_id for targeting)
- [x] Stage 22/24 divisions/groups integration documented
- [x] Gateway callback integration documented (stub + re-validate contract)

## Non-Goals / Exclusions

- [x] Cross-workspace promocodes: FORBIDDEN (confirmed)
- [x] Client-calculated discount: FORBIDDEN (confirmed)
- [x] Immutable fields update: FORBIDDEN (confirmed)
- [x] Usage history deletion: FORBIDDEN (confirmed)
- [x] Retroactive application: FORBIDDEN (confirmed)
- [x] MMC access to student-level promo data: FORBIDDEN (confirmed)
- [x] Bulk code generation / CSV import: out of scope (declared)
- [x] Frontoffice student-facing code submission: out of scope (admin-only this stage)
- [x] Gateway refund orchestration: out of scope (log + alert only)
