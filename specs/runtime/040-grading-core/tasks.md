# Tasks — Grading Core

**Stage:** STAGE_40_GRADING_CORE  
**Phase:** 03_BACKOFFICE_CORE / 04_EXAM_ENGINE_CORE  
**Generated:** 2026-04-02  
**Total Tasks:** 19

---

## Phase 1: Drizzle Schema Definitions

- [X] T001 Create `apps/api/src/db/tenant/schemas/grading-results.schema.ts` with full Drizzle schema: columns (id, workspace_id, attempt_id, total_score, total_possible_score, percentage, passed, pass_type, pass_value, grading_version, graded_at, graded_by, metadata, created_at), uniqueIndex on attempt_id, composite index on (workspace_id, attempt_id), CHECK constraints (valid_pass_type IN PERCENTAGE/SCORE; valid_graded_by IN ENGINE/SELF/ADMIN; valid_percentage_range 0–100); export GradingResult and NewGradingResult types
- [X] T002 Create `apps/api/src/db/tenant/schemas/grading-question-results.schema.ts` with full Drizzle schema: columns (id, workspace_id, attempt_id, grading_result_id, question_id, question_type, question_score, awarded_score, is_correct, user_response, correct_answer_snapshot, grading_metadata, created_at), uniqueIndex on (attempt_id, question_id), index on grading_result_id, composite index on (workspace_id, attempt_id), CHECK constraints (valid_question_type for all 7 types; valid_awarded_score >= 0 AND <= question_score); export GradingQuestionResult and NewGradingQuestionResult types
- [X] T003 Create `apps/api/src/db/tenant/schemas/grading-overrides.schema.ts` with full Drizzle schema: columns (id, workspace_id, attempt_id, grading_result_id, previous_score, new_score, previous_passed, new_passed, override_reason, override_user_id, created_at), index on attempt_id, index on workspace_id; export GradingOverride and NewGradingOverride types
- [X] T004 Modify `apps/api/src/db/tenant/schemas/attempts.schema.ts`: add `grading_status` varchar(20) column with default 'PENDING' after the `passed` field; add `check('valid_grading_status', sql\`...\`)`constraint for PENDING/GRADING/GRADED/OVERRIDE values; update existing`valid_status` check to include 'GRADED' in the allowed set; update file header comment to reference STAGE_40_GRADING_CORE; export updated Attempt and NewAttempt types
- [X] T005 Add barrel re-exports for all three new schemas to `apps/api/src/db/tenant/schemas/index.ts`: `export * from './grading-results.schema'`, `export * from './grading-question-results.schema'`, `export * from './grading-overrides.schema'`

## Phase 2: Migration

- [X] T006 Create `apps/api/src/db/tenant/migrations/20260404_019_grading_core.ts` with full migration: header comment block (stage, schema 1.24.0→1.25.0, description of all changes), `description` export string, `up(client)` function in single BEGIN/COMMIT: (1) ADD COLUMN grading_status to attempts with NOT NULL DEFAULT 'PENDING'; (2) ADD CONSTRAINT valid_grading_status check; (3) DROP CONSTRAINT valid_status and ADD CONSTRAINT valid_status with GRADED included; (4) CREATE TABLE grading_results with all columns, UNIQUE constraint, CHECK constraints; (5) CREATE TABLE grading_question_results with all columns, UNIQUE constraint, CHECK constraints; (6) CREATE TABLE grading_overrides with all columns; (7) CREATE all indexes (7 total); (8) UPDATE schema_versions SET version = '1.25.0'; `down()` stub as no-op with rollback note

## Phase 3: Domain-Core Types & Errors

- [X] T007 [P] Create `packages/domain-core/src/grading/grading.types.ts` with all shared type definitions: `DrizzleTransaction` alias, `GradeAttemptInput` (attemptId, workspaceId), `GradeAttemptResult` (gradingResultId, totalScore, totalPossibleScore, percentage, passed, questionResults[], gradingVersion, gradedAt), `QuestionGradingResult` (questionId, questionType, questionScore, awardedScore, isCorrect, userResponse, correctAnswerSnapshot, gradingMetadata?), `GradingConfigSnapshot` (questions[], pass_type, pass_value, total_possible_score, normalize_case?), `QuestionSnapshot` (id, type, score, correct_answer, question_category), `McqAnswerResponse` (selectedOptionId? | selectedOptionIds?[] | selectedValue? | orderedOptionIds?[]), `TraditionalAnswerResponse` (textAnswer? | selectedValue? | rawAnswer?, selfFlag?, awardedScore?), `GradingVersion` constant = '1.0.0'
- [X] T008 [P] Create `packages/domain-core/src/grading/grading.errors.ts` with: `GradingErrorCode` union type (7 codes: GRADING_ATTEMPT_NOT_FOUND, GRADING_ATTEMPT_ALREADY_GRADED, GRADING_SNAPSHOT_INCOMPLETE, GRADING_SCORE_OVERFLOW, GRADING_LATE_SUBMISSION, GRADING_INVALID_STATUS, GRADING_TRANSACTION_FAILED); `GRADING_ERROR_HTTP_STATUS` record mapping all codes to HTTP status codes (404/409/422/500); `GRADING_ERROR_MESSAGES` record with descriptive messages; `GradingError` class extending Error with code, httpStatus, message properties following existing McqExamError pattern

## Phase 4: Pure Grader Functions

- [X] T009 [P] Create `packages/domain-core/src/grading/mcq-grader.ts` with: `gradeMcqQuestion(snapshot: QuestionSnapshot, response: McqAnswerResponse): QuestionGradingResult` pure function handling all 4 MCQ types — SINGLE (strict equality on selectedOptionId), MULTIPLE (sorted array comparison for exact set match), TRUE_FALSE (boolean equality on selectedValue), ARRANGEMENT (index-by-index ordered array comparison on orderedOptionIds); all return full questionScore or 0; no partial scoring; no negative scoring; no side effects; include inline JSDoc for each type branch
- [X] T010 [P] Create `packages/domain-core/src/grading/traditional-grader.ts` with: `gradeTraditionalQuestion(snapshot: QuestionSnapshot, response: TraditionalAnswerResponse, config: { normalize_case?: boolean }): QuestionGradingResult` pure function handling 3 Traditional types — TRADITIONAL_TRUE_FALSE (boolean equality), FILL_BLANK (exact string match; if normalize_case=true lowercase+trim both sides before compare), SHORT_ANSWER (self-evaluated: store rawAnswer, selfFlag, use awardedScore directly as awarded value — score is student-supplied not computed); all return full questionScore or 0 except SHORT_ANSWER which returns explicit awardedScore; no side effects
- [X] T011 [P] Create `packages/domain-core/src/grading/score-aggregator.ts` with: `aggregateScores(questionResults: QuestionGradingResult[], config: { pass_type: 'PERCENTAGE' | 'SCORE'; pass_value: number; total_possible_score: number }): { totalScore: number; totalPossibleScore: number; percentage: number; passed: boolean }` pure function — totalScore = SUM(awardedScore), percentage = (totalScore / totalPossibleScore) \* 100 rounded to 2 decimal places, passed = pass_type===PERCENTAGE ? percentage >= pass_value : totalScore >= pass_value; handle edge case of totalPossibleScore = 0 (percentage = 0, passed = false); no side effects

## Phase 5: Grading Repository

- [X] T012 Create `packages/domain-core/src/grading/grading.repository.ts` with all DB operations using same `tx` handle: `findAttemptForGrading(tx, attemptId, workspaceId)` using SELECT FOR UPDATE to lock the row, `insertGradingResult(tx, data)` INSERT INTO grading_results returning id, `insertGradingQuestionResults(tx, rows[])` batch INSERT INTO grading_question_results using Drizzle's `values()`, `updateAttemptGradingStatus(tx, attemptId, workspaceId, status)` UPDATE attempts SET grading_status, `finalizeAttemptGrading(tx, attemptId, workspaceId, result)` UPDATE attempts SET grading_status='GRADED' AND status='GRADED' AND score=percentage AND passed=passed AND finalized_at=NOW(), `findExistingGradingResult(tx, attemptId, workspaceId)` SELECT from grading_results WHERE attempt_id + workspace_id; all queries scope by workspace_id for tenant isolation

## Phase 6: Grading Engine Orchestrator

- [X] T013 Create `packages/domain-core/src/grading/grading-engine.ts` with: `gradeAttempt(input: GradeAttemptInput, tx: DrizzleTransaction): Promise<GradeAttemptResult>` implementing the full 13-step transactional flow from plan.md section 5 — (1) findAttemptForGrading (SELECT FOR UPDATE), (2) validate status ∈ SUBMITTED/IN_PROGRESS-auto-submit, (3) idempotency check grading_status≠GRADED, (4) validate snapshot completeness (all required fields present), (5) updateAttemptGradingStatus to GRADING, (6) dispatch each question to mcqGrader or traditionalGrader based on question type, (7) aggregateScores, (8) insertGradingResult, (9) insertGradingQuestionResults batch, (10) finalizeAttemptGrading, (11) structured logger.info with all required observability fields; on idempotency hit return findExistingGradingResult mapped to GradeAttemptResult; all errors thrown as GradingError; import logger from @zidney/logger; record start time and compute grading_duration_ms; export constant GRADING_VERSION = '1.0.0'

## Phase 7: Barrel Exports

- [X] T014 Create `packages/domain-core/src/grading/index.ts` barrel that exports: `gradeAttempt` from grading-engine, `GRADING_VERSION` from grading-engine, `GradingError` from grading.errors, `GRADING_ERROR_HTTP_STATUS` from grading.errors, `GradingErrorCode` type from grading.errors; and type-only exports for all types from grading.types
- [X] T015 Update `packages/domain-core/src/index.ts` to add `export * from './grading'` export alongside existing module exports

## Phase 8: Unit Tests

- [X] T016 [P] Create `packages/domain-core/src/grading/__tests__/mcq-grader.test.ts` with vitest unit tests covering all 4 MCQ types: SINGLE correct/incorrect (6 cases), MULTIPLE exact set match/partial/empty (9 cases), TRUE_FALSE true/false correct/incorrect (4 cases), ARRANGEMENT exact order/wrong order/partial (6 cases); assert returned awardedScore, isCorrect, questionScore for each; test that pure function has no side effects (same input → same output on repeated calls); 25+ assertions total
- [X] T017 [P] Create `packages/domain-core/src/grading/__tests__/traditional-grader.test.ts` with vitest unit tests: TRADITIONAL_TRUE_FALSE correct/incorrect (4 cases), FILL_BLANK exact match/no match/case-sensitive/case-normalized (8 cases), SHORT_ANSWER with various awardedScore values including zero and max (6 cases); assert awardedScore, isCorrect, userResponse stored correctly; 18+ assertions
- [X] T018 [P] Create `packages/domain-core/src/grading/__tests__/score-aggregator.test.ts` with vitest unit tests: all correct (100%), all wrong (0%), mixed results, pass by PERCENTAGE type (threshold met/not met), pass by SCORE type (threshold met/not met), edge case totalPossibleScore=0, single question, determinism (same input → same output); 20+ assertions

## Phase 9: Integration Tests

- [X] T019 Create `packages/domain-core/src/grading/__tests__/grading-engine.test.ts` with vitest integration tests using real tenant DB test connection (following existing test patterns in packages/domain-core/src/): full grading flow for MCQ attempt (insert attempt rows, call gradeAttempt, assert grading_results and grading_question_results rows persisted, assert attempt.grading_status=GRADED, attempt.status=GRADED), idempotent re-grade (call gradeAttempt twice, assert no duplicate rows, second call returns existing result), invalid status rejection (attempt in IN_PROGRESS → assert GRADING_INVALID_STATUS), missing snapshot rejection (attempt with incomplete grading_config_snapshot → assert GRADING_SNAPSHOT_INCOMPLETE), already-graded guard (attempt.grading_status=GRADED → assert GRADING_ATTEMPT_ALREADY_GRADED), transaction rollback validation (mock engine step to throw → assert attempt remains in previous state with no partial grading rows)
