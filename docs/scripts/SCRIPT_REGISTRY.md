# Zidney Script Registry

> Auto-generated by `bun run dev:generate:script-docs`. Do not edit manually.
> Last generated: 2026-04-02T17:47:37.149Z

## ai

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `ai:context:generate` | `scripts/generate-ai-context.ts` | dev | Generate AI context artifacts (architecture brain, module map, dependency graph). Supports incremental (default), forced, and validate-after-generation modes. | `bun run ai:context:generate` |
| `ai:guard` | `scripts/ai-guard.ts` | governance | Enforces architecture rules (import boundaries, contract compliance) before AI-generated commits and in CI. | `bun run ai:guard` |
| `ai:plan` | `scripts/ai-engine/plan-task.ts` | dev | Build a deterministic execution plan for an AI task and write the plan artifact alongside an execution log. | `bun run ai:plan --task "<task-description>"` |
| `ai:run` | `scripts/ai-engine/run-task.ts` | dev | Execute AI task orchestration with governance validation, execution logging, and deterministic exit codes. | `bun run ai:run --task "<task-description>"` |
| `ai:runtime:status` | `scripts/ai-runtime/runtime-status.ts` | dev | Check the AI runtime environment layers, report health status, and exit non-zero when runtime prerequisites are broken. | `bun run ai:runtime:status` |
| `ai:validate` | `scripts/ai-engine/validate-execution.ts` | governance | Validate AI execution prerequisites and post-change governance checks, then write a deterministic execution log. | `bun run ai:validate` |
| `ai:validate:prompts` | `scripts/prompt-qa.ts` | validation | Validates AI agent and prompt file structural integrity | `bun run ai:validate:prompts` |

## arch

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `arch:add-module` | `scripts/architecture/add-module.ts` | dev | Register a new module path in ARCHITECTURE_MAP.json with the default architecture metadata scaffold. | `bun run arch:add-module <module-path>` |
| `arch:audit` | `scripts/infra-audit.ts` | governance | Monorepo governance scanner — audits Vitest, ESLint, Playwright, import boundaries, and outputs infra-audit-report.json. | `bun run arch:audit` |
| `arch:check:store-cycles` | `scripts/check-store-cycles.ts` | governance | Runs madge on each app's src/core/state/ to assert zero circular dependencies. Exits with non-zero code on any detected cycle. | `bun run arch:check:store-cycles` |
| `arch:context:build` | `scripts/context/build.ts` | governance | Generates docs/ai/context/gitnexus-context.json via assembleContext(). Uses atomic write (write to .tmp then renameSync) to prevent partial artifact state. Supports --dry-run (print to stdout only), --all (full workspace), --force (skip freshness check and always regenerate). | `bun run arch:context:build [-- --dry-run] [-- --all] [-- --force]` |
| `arch:context:changed` | `scripts/context/changed.ts` | governance | Resolves staged changed files via `git diff --cached` and writes the result to docs/ai/context/context-changed.json with a 5-minute freshness cache. Subsequent reads within the cache window skip the git invocation. A clean staging area (no changed files) is a valid state — the artifact is written with an empty changedFiles array rather than exiting non-zero. | `bun run arch:context:changed` |
| `arch:context:impact` | `scripts/context/impact.ts` | governance | Synthesizes risk indicators from docs/ai/context/gitnexus-context.json, filtered by the staged changed files recorded in context-changed.json. A risk indicator is included when its `affectedBy` set intersects the staged changed files. Writes the result to docs/ai/context/context-impact.json. If context-changed.json does not exist, falls back to the `changedFiles` array embedded in the main context artifact. Output mode: (default)  one `indicator.module` per line, sorted alphabetically --json     full riskIndicators array as JSON on stdout | `bun run arch:context:impact [-- --json]` |
| `arch:context:validate` | `scripts/context/validate.ts` | governance | Validates docs/ai/context/gitnexus-context.json against docs/ai/gitnexus-context.schema.json. No external schema library (NFR-005). Validation order (stops at first failure): 1. Artifact file exists 2. Valid JSON 3. Schema file exists and is readable 4. All required fields present 5. schemaVersion matches schema.version 6. generatedAt is < maxAgeHours old (default: 24h) | `bun run arch:context:validate` |
| `arch:diff` | `scripts/architecture-diff.ts` | governance | Detect architecture violations in the current change set (PR/staged diff) by comparing changed imports against ARCHITECTURE_CONTRACT.json rules. | `bun run arch:diff` |
| `arch:generate` | `scripts/architecture/generate-architecture-map.ts` | dev | Regenerate ARCHITECTURE_MAP.json by scanning top-level apps and packages modules and their dependencies. | `bun run arch:generate` |
| `arch:gitnexus:context` | `scripts/gitnexus-context.ts` | dev | Generates a structured GitNexus context JSON artifact from git state and ai-architecture-brain.json for AI orchestrators and CI gates. | `bun run arch:gitnexus:context` |
| `arch:gitnexus:validate` | `scripts/validate/validate-gitnexus.ts` | governance | Validates the gitnexus-context.json artifact for file presence, structure, semantics, and freshness. | `bun run arch:gitnexus:validate` |
| `arch:guard` | `scripts/architecture-guard/architecture-guard.ts` | governance | Run the unified architecture guard and exit non-zero when governance violations are detected. | `bun run arch:guard` |
| `arch:health` | `scripts/architecture-health/architecture-health.ts` | governance | Run the architecture health assessment pipeline and write the health report artifacts. | `bun run arch:health` |
| `arch:health:benchmark` | `scripts/architecture-health/benchmark.ts` | dev | Benchmark the architecture health runner across repeated executions and report the p95 budget result. | `bun run arch:health:benchmark` |
| `arch:type-safety-guard` | `scripts/type-safety-guard.ts` | governance | Unified architecture guard — runs type safety checks and validates import boundaries using the architecture contract. | `bun run arch:type-safety-guard` |
| `arch:validate:brain` | `scripts/governance/validate-architecture-brain.ts` | governance | Validate the architecture brain artifact structure and dependency paths before it is committed or consumed. | `bun run arch:validate:brain` |
| `arch:visualize` | `scripts/architecture/visualize.ts` | dev | Generate Mermaid architecture diagrams and documentation from the dependency graph and architecture map artifacts. | `bun run arch:visualize` |

## ci

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `ci:run-local` | `scripts/run-local-ci.ts` | dev | Local CI governance orchestrator — runs the 7-step pre-closure validation sequence including all governance checks and the full act CI simulation. Step 0 is a Docker fail-fast check; Steps 1–7 are governance checks that run to completion regardless of individual failures (fail-forward). | `bun run ci:run-local` |

## db

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `db:console` | `scripts/db/console.ts` | runtime | Launch an interactive psql session connected to DATABASE_URL. | `bun run db:console` |
| `db:migrate` | `scripts/db/migrate.ts` | runtime | Validate migration inputs and delegate master migration execution guidance. | `bun run db:migrate` |
| `db:status:pool` | `scripts/db/pool-status.ts` | runtime | Check PostgreSQL connection pool health and report status. | `bun run db:status:pool` |
| `db:validate:licenses` | `scripts/db/validate-licenses.ts` | governance | Validate license distribution in master_db and report counts by status. | `bun run db:validate:licenses` |

## dev

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `dev:ai:archive-snapshots` | `scripts/dev/archive-snapshot-strategy.ts` | dev | Maintain a rolling archive of historical AI context snapshots, keeping the latest artifacts live and archiving older ones with indexes. | `bun run dev:ai:archive-snapshots` |
| `dev:ai:context-artifacts` | `scripts/dev/analyze-ai-context-artifacts.ts` | dev | Analyze AI context artifacts for size, compression, and redundancy to identify Phase 3 optimization opportunities. | `bun run dev:ai:context-artifacts` |
| `dev:analyze:directory-sizes` | `scripts/dev/analyze-directory-sizes.ts` | dev | Measure key repository directory sizes and highlight the largest contributors to repository bloat. | `bun run dev:analyze:directory-sizes` |
| `dev:analyze:file-sizes` | `scripts/dev/analyze-file-sizes.ts` | dev | Analyze repository files by size and line count to identify oversized files that need optimization. | `bun run dev:analyze:file-sizes` |
| `dev:benchmark:ci` | `scripts/dev/benchmark-ci-duration.ts` | dev | Benchmark CI pipeline duration assumptions, generate markdown performance reports, and write a dashboard snapshot. | `bun run dev:benchmark:ci` |
| `dev:demo:logger` | `scripts/dev/demo-logger-features.ts` | dev | Demonstrates all available logger customization options and features | `bun run dev:demo:logger` |
| `dev:deps:verify` | `scripts/dev/verify-dependency-usage.ts` | dev | Scan the codebase for dependency usage patterns and produce a conservative unused-dependency candidate report. | `bun run dev:deps:verify` |
| `dev:generate:package-docs` | `scripts/generate/package-docs.ts` | dev | Refresh root package.md from package.json, repo invocation scans, and optional detached-worktree audit evidence. | `bun run dev:generate:package-docs [-- --audit=ai:validate,arch:guard:ci]` |
| `dev:generate:script-docs` | `scripts/generate/script-docs.ts` | dev | Walk scripts/**\/*.ts, parse @script metadata headers, generate docs/scripts/SCRIPT_REGISTRY.md plus one page per root package.json runner. Supports --check-only to validate the current docs state without writing files. Exits 1 on missing required metadata fields or docs drift in check-only mode. | `bun run dev:generate:script-docs [-- --check-only] [--ci]` |
| `dev:hygiene:report` | `scripts/dev/hygiene-report-generator.ts` | dev | Run repository hygiene checks and write a structured Markdown report without failing the process on report findings. | `bun run dev:hygiene:report` |
| `dev:profile:scripts` | `scripts/dev/profile-script-performance.ts` | dev | Profile governance script execution times across repeated runs to establish a performance baseline. | `bun run dev:profile:scripts [runs=10]` |
| `dev:refactor:scripts` | `scripts/dev/refactor-scripts.ts` | dev | Applies the migration map to rename all "bun run <old>" references across the repository. Supports both JSON format (docs/scripts/migration-map.json, preferred) and Markdown format (docs/scripts/SCRIPT_MIGRATION_MAP.md, legacy). Builds the old→new rename index, then rewrites all matching files in-place. Supports --dry-run to preview changes without writing. Exits 1 if any unresolved references remain. Writes reports to both reports/SCRIPT_REFACTOR_REPORT.md and docs/reports/script-refactor-report.json. | `bun run refactor-scripts [--dry-run]` |
| `dev:report:baseline` | `scripts/dev/generate-baseline-report.ts` | dev | Generate a consolidated baseline diagnostics report for Phase 1 analysis and write it to docs/audit-reports. | `bun run dev:report:baseline` |
| `dev:seed:dashboard-test-data` | `scripts/seed/dashboard-test-data.ts` | runtime | Seed realistic MMC dashboard test data into master_db for dashboard testing | `bun run dev:seed:dashboard-test-data` |
| `dev:validate:script-duplication` | `scripts/dev/validate-script-duplication.ts` | dev | Measure cross-file duplication within the scripts directory and validate that modularization stays within the target threshold. | `bun run dev:validate:script-duplication` |

## governance

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `governance:gate` | `scripts/governance/gate.ts` | governance | Unified governance gate — composes all guards in sequence (report-all mode) | `bun run governance:gate` |
| `governance:gate:ci` | `scripts/governance/gate-ci.ts` | governance | CI variant of the governance gate — runs gate.ts with GitHub Actions annotations | `bun run governance:gate:ci` |
| `governance:report` | `scripts/governance/report.ts` | governance | Generates a consolidated governance health report at docs/governance/governance-report.md | `bun run governance:report` |

## infra

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `infra:cache:clean` | `scripts/maintenance/cache-clean.ts` | maintenance | Remove build caches and temporary output directories to free disk space | `bun run infra:cache:clean` |
| `infra:security` | `scripts/security/scan.ts` | analysis | Run a full Trivy filesystem scan and print visible findings without blocking on non-clean results. | `bun run infra:security` |
| `infra:security:ci` | `scripts/security/scan-ci.ts` | analysis | Run the CI-equivalent Trivy scan, write a sanitized report, and block on CI-grade findings. | `bun run infra:security:ci` |
| `infra:security:config` | `scripts/security/scan-config.ts` | analysis | Run a Trivy misconfiguration scan and print visible findings without blocking on non-clean results. | `bun run infra:security:config` |
| `infra:security:deps` | `scripts/security/scan-deps.ts` | analysis | Run a dependency-only Trivy scan, warning on MEDIUM findings and blocking on HIGH/CRITICAL vulnerabilities. | `bun run infra:security:deps` |
| `infra:security:secrets` | `scripts/security/scan-secrets.ts` | analysis | Run a Trivy secret scan across the repo or staged files only and block on any detected secret. | `bun run infra:security:secrets [--staged]` |

## policy

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `policy:check` | `scripts/policy-engine/cli.ts` | governance | Run governance policy checks for changed files or the full repository and report the findings through the selected reporter. | `bun run policy:check` |

## repo

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `repo:doctor` | `scripts/dev/repo-doctor.ts` | dev | Run seven repository health checks and report pass, warn, and fail status for local diagnostics. | `bun run repo:doctor` |
| `repo:fix` | `scripts/dev/repo-fix.ts` | dev | Run automated repository repair steps, continue through failures, and report the overall repair result. | `bun run repo:fix` |
| `repo:onboard` | `scripts/dev/repo-onboard.ts` | dev | Prepare a complete local development environment in one command, including dependency install, hooks, and core service checks. | `bun run repo:onboard` |
| `repo:status` | `scripts/dev/repo-status.ts` | dev | Render a read-only summary of repository health indicators without modifying any workspace files. | `bun run repo:status` |

## validate

| Script Name | Source File | Category | Description | Usage |
| ----------- | ----------- | -------- | ----------- | ----- |
| `validate:ai-context-fresh` | `scripts/validate/ai-context-fresh.ts` | validation | Check that the AI context mini artifact exists and is not older than 24 hours | `bun run validate:ai-context-fresh` |
| `validate:ai-context-schemas` | `scripts/validate/ai-context-schemas.ts` | validation | Validate that all required AI context JSON artifacts exist and are valid JSON | `bun run validate:ai-context-schemas` |
| `validate:scan:packages` | `scripts/validate/scan-package-scripts.ts` | governance | Walk all runtime spec docs and extract unique script references | `bun run validate:scan:packages` |
| `validate:scripts:all` | `scripts/validate/index.ts` | governance | Orchestrator that runs all script-system validators sequentially. Composes: runtime-scripts, detect-broken-scripts, spec-sync, docs-drift. Also enforces the Script Evolution Guard: if package.json scripts changed in the current git diff, the migration-map must have been updated too. Exits non-zero on the first failure. Prints a timing summary. | `bun run validate:scripts:all` |
| `validate:scripts:broken` | `scripts/validate/detect-broken-scripts.ts` | governance | Detect missing or broken TypeScript script files referenced in root package.json | `bun run validate:scripts:all` |
| `validate:scripts:docs-drift` | `scripts/validate/docs-drift.ts` | governance | Detects scripts in root package.json that have no corresponding documentation file in docs/scripts/. Reports missing docs and exits non-zero when undocumented scripts are found. | `bun run validate:scripts:docs-drift` |
| `validate:scripts:infrastructure` | `scripts/validate/script-infrastructure.ts` | governance | Validates that all scripts/*.ts files have the mandatory 5-field metadata header (@script, @domain, @category, @description, @usage) and that the SCRIPT_REGISTRY.md is up-to-date (no drift vs. what the generator would produce). Reports all violations before exiting non-zero. | `bun run validate:scripts:infrastructure` |
| `validate:scripts:naming` | `scripts/validate/script-naming.ts` | governance | Validates all package.json script keys conform to the <domain>:<action>[:<scope>] naming convention. Allowed domains: db, arch, validate, ai, ci, repo, dev, infra, test, governance, policy. Lifecycle-exempt names and category comments (scripts starting with "_") are skipped. Reports ALL violations before exiting non-zero. | `bun run validate:scripts:naming` |
| `validate:scripts:runtime` | `scripts/validate/runtime-scripts.ts` | governance | CI guard: hard-blocks (exit 1) when any bun run <script> reference in the project (outside specs, .gitnexus, and reports) is absent from root package.json. References inside those dirs generate warnings but exit 0. Exits 0 when no critical issues found. | `bun run validate:scripts:all` |
| `validate:scripts:spec-sync` | `scripts/validate/spec-sync.ts` | governance | Reverse validation: scans all spec files (specs/, docs/) for `bun run <script>` references and verifies each exists in root package.json. Exits non-zero if any referenced script is missing. | `bun run validate:scripts:spec-sync` |
| `validate:scripts:usage` | `scripts/validate/script-usage.ts` | governance | Scans all .ts, .json, .yml, .yaml, .md, and .sh files for "bun run <name>" references and validates that every referenced name exists in a package.json scripts block. Reports all broken/orphan references before exiting non-zero. | `bun run validate:scripts:usage` |
| `validate:scripts:ux` | `scripts/validate/validate-scripts-ux.ts` | governance | Validates scripts for consistent UX, logging, and exit usage. | `bun run validate:scripts:ux` |
